# =============================================================================
# Selgis ML - Requirements
# =============================================================================
# Usage:
#   pip install -r requirements.txt           # Base version
#   pip install -r requirements-dev.txt       # Development
#   pip install -r requirements-all.txt       # Full version (all)
# =============================================================================

# -----------------------------------------------------------------------------
# BASE REQUIREMENTS (mandatory)
# -----------------------------------------------------------------------------
torch>=2.0.0
numpy>=1.20.0
tqdm>=4.65.0
